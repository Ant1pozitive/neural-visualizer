# Interactive Neural Network Training Visualization — Frontend

## Development

1. Install dependencies
   ```bash
   cd frontend
   npm install
   ```

2. Start dev server
   ```bash
   npm run dev
   ```

3. Open http://localhost:5173

Backend API is expected at http://localhost:8000 by default. Adjust `src/api/client.ts` if needed.
